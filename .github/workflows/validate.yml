# THIS FILE IS AUTO-GENERATED by vibe-validate generate-workflow
# DO NOT EDIT MANUALLY - Edit vibe-validate.config.yaml instead
# Regenerate with: npx vibe-validate generate-workflow
#
# Source of truth: vibe-validate.config.yaml
name: Validation Pipeline
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  typescript-type-check:
    name: TypeScript Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run typecheck
  eslint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run lint
  openapi-validation:
    name: OpenAPI Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:openapi
  build-packages:
    name: Build Packages
    runs-on: ubuntu-latest
    needs:
      - typescript-type-check
      - eslint
      - openapi-validation
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run build
  security-contract-tests:
    name: Security & Contract Tests
    runs-on: ubuntu-latest
    needs: &a1
      - build-packages
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run security:check && npm run test:contract:local
  unit-integration-tests:
    name: Unit & Integration Tests
    runs-on: ubuntu-latest
    needs: *a1
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:unit && npm run test:integration
  system-tests-stdio:
    name: System Tests (STDIO)
    runs-on: ubuntu-latest
    needs: *a1
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:system:stdio
  system-tests-http:
    name: System Tests (HTTP)
    runs-on: ubuntu-latest
    needs: *a1
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:system:ci
  headless-browser-tests:
    name: Headless Browser Tests
    runs-on: ubuntu-latest
    needs: *a1
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium && npm run test:system:headless
  all-validation-passed:
    name: All Validation Passed
    runs-on: ubuntu-latest
    needs:
      - typescript-type-check
      - eslint
      - openapi-validation
      - build-packages
      - security-contract-tests
      - unit-integration-tests
      - system-tests-stdio
      - system-tests-http
      - headless-browser-tests
    if: always()
    steps:
      - name: Check all validation jobs
        run: >-
          if [ "${{ needs.typescript-type-check.result }}" != "success" ] || \
                       [ "${{ needs.eslint.result }}" != "success" ] || \
                       [ "${{ needs.openapi-validation.result }}" != "success" ] || \
                       [ "${{ needs.build-packages.result }}" != "success" ] || \
                       [ "${{ needs.security-contract-tests.result }}" != "success" ] || \
                       [ "${{ needs.unit-integration-tests.result }}" != "success" ] || \
                       [ "${{ needs.system-tests-stdio.result }}" != "success" ] || \
                       [ "${{ needs.system-tests-http.result }}" != "success" ] || \
                       [ "${{ needs.headless-browser-tests.result }}" != "success" ]; then
                      echo "❌ Some validation checks failed"
                      exit 1
                    fi
                    echo "✅ All validation checks passed!"
