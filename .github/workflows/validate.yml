# THIS FILE IS AUTO-GENERATED by tools/generate-validate-workflow.ts
# DO NOT EDIT MANUALLY - Edit tools/validation-config.ts instead
# Regenerate with: npm run validate:generate-workflow
#
# Source of truth: tools/validation-config.ts
# Generated: 2025-10-12T13:45:49.611Z
name: Validation Pipeline
"on":
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
jobs:
  typecheck:
    name: TypeScript type checking
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - run: npm ci
      - run: npm run typecheck
        env:
          LLM_OUTPUT: "1"
  lint:
    name: ESLint code checking
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - run: npm ci
      - run: npm run lint
        env:
          LLM_OUTPUT: "1"
  openapi:
    name: OpenAPI validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - run: npm ci
      - run: npm run test:openapi
        env:
          LLM_OUTPUT: "1"
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          LLM_OUTPUT: "1"
  unit-tests:
    name: Unit tests
    runs-on: ubuntu-latest
    needs:
      - typecheck
      - lint
      - openapi
      - build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - run: npm ci
      - run: npm run test:unit
        env:
          LLM_OUTPUT: "1"
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
  integration-tests:
    name: Integration tests
    runs-on: ubuntu-latest
    needs:
      - typecheck
      - lint
      - openapi
      - build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - run: npm ci
      - run: npm run test:integration
        env:
          LLM_OUTPUT: "1"
  stdio-tests:
    name: STDIO system tests
    runs-on: ubuntu-latest
    needs:
      - typecheck
      - lint
      - openapi
      - build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - run: npm ci
      - run: npm run test:system:stdio
        env:
          LLM_OUTPUT: "1"
  http-tests:
    name: HTTP system tests
    runs-on: ubuntu-latest
    needs:
      - typecheck
      - lint
      - openapi
      - build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - run: npm ci
      - run: npm run test:system:ci
        env:
          LLM_OUTPUT: "1"
  headless-tests:
    name: Headless browser tests
    runs-on: ubuntu-latest
    needs:
      - typecheck
      - lint
      - openapi
      - build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: npm
      - run: npm ci
      - name: Install Playwright browsers
        run: npx playwright install --with-deps
      - run: npm run test:system:headless
        env:
          LLM_OUTPUT: "1"
  all-validation-passed:
    name: All Validation Passed
    runs-on: ubuntu-latest
    needs:
      - typecheck
      - lint
      - openapi
      - build
      - unit-tests
      - integration-tests
      - stdio-tests
      - http-tests
      - headless-tests
    if: always()
    steps:
      - name: Check all validation jobs
        run: |-
          if [ "${{ needs.typecheck.result }}" != "success" ] || \
                       [ "${{ needs.lint.result }}" != "success" ] || \
                       [ "${{ needs.openapi.result }}" != "success" ] || \
                       [ "${{ needs.build.result }}" != "success" ] || \
                       [ "${{ needs.unit-tests.result }}" != "success" ] || \
                       [ "${{ needs.integration-tests.result }}" != "success" ] || \
                       [ "${{ needs.stdio-tests.result }}" != "success" ] || \
                       [ "${{ needs.http-tests.result }}" != "success" ] || \
                       [ "${{ needs.headless-tests.result }}" != "success" ]; then
                      echo "❌ Some validation checks failed"
                      exit 1
                    fi
                    echo "✅ All validation checks passed!"
