# THIS FILE IS AUTO-GENERATED by vibe-validate generate-workflow
# DO NOT EDIT MANUALLY - Edit vibe-validate.config.yaml instead
# Regenerate with: npx vibe-validate generate-workflow
#
# Source of truth: vibe-validate.config.yaml
name: Validation Pipeline
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  fast-checks-build:
    name: Fast Checks & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Build packages
        run: npm run build
      - name: TypeScript Type Check
        run: npm run typecheck
      - name: ESLint
        run: npm run lint
      - name: OpenAPI Validation
        run: npm run test:openapi
      - name: Security Check
        run: npm run security:check
      - name: Wildcard Dependencies Check
        run: npm run validate:wildcards
      - name: Contract Tests
        run: npm run test:contract:local
      - name: Build Packages
        run: npm run build
      - name: Unit Tests
        run: npm run test:unit
      - name: Integration Tests
        run: npm run test:integration
  system-tests-stdio:
    name: System Tests (STDIO)
    runs-on: ubuntu-latest
    needs: &a1
      - fast-checks-build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:system:stdio
  system-tests-http:
    name: System Tests (HTTP)
    runs-on: ubuntu-latest
    needs: *a1
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:system:ci
  headless-browser-tests:
    name: Headless Browser Tests
    runs-on: ubuntu-latest
    needs: *a1
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium && npm run test:system:headless
  all-validation-passed:
    name: All Validation Passed
    runs-on: ubuntu-latest
    needs:
      - fast-checks-build
      - system-tests-stdio
      - system-tests-http
      - headless-browser-tests
    if: always()
    steps:
      - name: Check all validation jobs
        run: >-
          if [ "${{ needs.fast-checks-build.result }}" != "success" ] || \
                       [ "${{ needs.system-tests-stdio.result }}" != "success" ] || \
                       [ "${{ needs.system-tests-http.result }}" != "success" ] || \
                       [ "${{ needs.headless-browser-tests.result }}" != "success" ]; then
                      echo "❌ Some validation checks failed"
                      exit 1
                    fi
                    echo "✅ All validation checks passed!"
