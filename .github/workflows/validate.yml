# THIS FILE IS AUTO-GENERATED by vibe-validate generate-workflow
# DO NOT EDIT MANUALLY - Edit vibe-validate.config.mjs instead
# Regenerate with: npx vibe-validate generate-workflow
#
# Source of truth: vibe-validate.config.mjs
name: Validation Pipeline
"on":
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  typescript-type-checking:
    name: TypeScript type checking
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run typecheck
  eslint-code-checking:
    name: ESLint code checking
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run lint
  openapi-validation:
    name: OpenAPI validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:openapi
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run build
  unit-tests:
    name: Unit tests
    runs-on: ubuntu-latest
    needs:
      - typescript-type-checking
      - eslint-code-checking
      - openapi-validation
      - build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:unit
  integration-tests:
    name: Integration tests
    runs-on: ubuntu-latest
    needs:
      - typescript-type-checking
      - eslint-code-checking
      - openapi-validation
      - build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:integration
  stdio-system-tests:
    name: STDIO system tests
    runs-on: ubuntu-latest
    needs:
      - typescript-type-checking
      - eslint-code-checking
      - openapi-validation
      - build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:system:stdio
  http-system-tests:
    name: HTTP system tests
    runs-on: ubuntu-latest
    needs:
      - typescript-type-checking
      - eslint-code-checking
      - openapi-validation
      - build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:system:ci
  headless-browser-tests:
    name: Headless browser tests
    runs-on: ubuntu-latest
    needs:
      - typescript-type-checking
      - eslint-code-checking
      - openapi-validation
      - build
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run test:system:headless
  all-validation-passed:
    name: All Validation Passed
    runs-on: ubuntu-latest
    needs:
      - typescript-type-checking
      - eslint-code-checking
      - openapi-validation
      - build
      - unit-tests
      - integration-tests
      - stdio-system-tests
      - http-system-tests
      - headless-browser-tests
    if: always()
    steps:
      - name: Check all validation jobs
        run: |-
          if [ "${{ needs.typescript-type-checking.result }}" != "success" ] || \
                       [ "${{ needs.eslint-code-checking.result }}" != "success" ] || \
                       [ "${{ needs.openapi-validation.result }}" != "success" ] || \
                       [ "${{ needs.build.result }}" != "success" ] || \
                       [ "${{ needs.unit-tests.result }}" != "success" ] || \
                       [ "${{ needs.integration-tests.result }}" != "success" ] || \
                       [ "${{ needs.stdio-system-tests.result }}" != "success" ] || \
                       [ "${{ needs.http-system-tests.result }}" != "success" ] || \
                       [ "${{ needs.headless-browser-tests.result }}" != "success" ]; then
                      echo "❌ Some validation checks failed"
                      exit 1
                    fi
                    echo "✅ All validation checks passed!"
